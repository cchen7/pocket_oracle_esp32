# Pocket Oracle — generic defaults (all targets)
# Target-specific tuning lives in sdkconfig.defaults.esp32s3.

# Partition table
CONFIG_PARTITION_TABLE_CUSTOM=y
CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="partitions.csv"
CONFIG_PARTITION_TABLE_OFFSET=0x8000

# FreeRTOS
CONFIG_FREERTOS_HZ=1000

# Bluetooth: NimBLE only (no Bluedroid)
CONFIG_BT_ENABLED=y
CONFIG_BT_NIMBLE_ENABLED=y
CONFIG_BT_NIMBLE_EXT_ADV=n
CONFIG_BT_NIMBLE_HID_SERVICE=y
CONFIG_BT_NIMBLE_MAX_CONNECTIONS=2

# WiFi (uses ESP-IDF defaults; pocket-oracle calls esp_wifi_stop when idle
# to free memory, so static/dynamic buffer tuning isn't worth the config risk)


# Logging (production: default WARN, strip VERBOSE/DEBUG/INFO+ from
# compiled binary entirely. Dev: edit to LEVEL_INFO + MAXIMUM_VERBOSE
# and rebuild.)
CONFIG_LOG_DEFAULT_LEVEL_WARN=y
CONFIG_LOG_MAXIMUM_LEVEL_INFO=y
CONFIG_BOOTLOADER_LOG_LEVEL_WARN=y

# NVS
CONFIG_NVS_ASSERT_ERROR_CHECK=n

# Compiler
CONFIG_COMPILER_OPTIMIZATION_PERF=y
CONFIG_COMPILER_OPTIMIZATION_ASSERTIONS_DISABLE=n
CONFIG_COMPILER_CXX_EXCEPTIONS=n

# LVGL — use our project-local lv_conf.h instead of Kconfig-generated stub
CONFIG_LV_CONF_SKIP=n

