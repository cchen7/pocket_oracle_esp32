# PRD — Pocket Oracle ESP32

> v0.1 · 2026-05-24

## 一、产品定位

一个开源硬件「轻决策包挂」。形态是可挂在书包/钥匙圈上的小屏设备，提供：
- 帮你做小决定的随机性玩法（答案之书、骰子、硬币、yes/no）
- 每日仪式感（运势、MBTI 提示）
- 顺手的小工具（时钟、电子木鱼）
- 蓝牙翻页器（PPT / 相机快门 / 音乐控制）

灵感来自少数派 × 思维重置「口袋先知」，但完全开源，硬件用现成的 M5Stack StickS3，便于复制和改造。

## 二、目标用户

- 喜欢小玩意、有"电子小垃圾"情怀的开发者
- 想要"轻决策"工具但不想下个 App 又多一份信息流的人
- ESP32/M5Stack 玩家，想要看一份"高完成度"的产品级开源参考

## 三、硬件

M5Stack StickS3（详见 `docs/HARDWARE_M5STICKS3.md`）。

**关键不可改项**：
- 1.14" 彩色 IPS LCD（**非墨水屏**，是与原版口袋先知最大的差异）
- 板载 250mAh 电池
- 2 颗物理按键 + 6 轴 IMU（可识别摇一摇）
- WiFi + BLE
- USB-C 充电/烧录

**硬件层面缺失（与原版差异）**：
- 无 NFC（V1 不做；后期可通过 PORT.A 外接）
- 体积比 AirPods 盒略大（48×24×15mm）

## 四、功能清单

### 4.1 随机决策（5 个 app）

| 应用 | 行为 | 输出 |
|---|---|---|
| 答案之书 | 350 条中文 + 350 条英文随机答案池 | 单条文字，逐字浮现动画 |
| 翻硬币 | 摇一摇/按键触发，模拟翻转 | "正"/"反" |
| 掷骰子 | KEY2 切换 1/3/5/9 个骰子，摇一摇/按键投掷 | 点数 + 总和 |
| 十以内 | 触发 → 1-10 随机数字 | 48pt 大字 |
| 快速决策 | 触发 → Yes/No | 极简两字 |

### 4.2 仪式 / 运势（2 个 app）

| 应用 | 行为 | 输出 |
|---|---|---|
| MBTI 人格指南 | 显示当前 MBTI + 今日提示；KEY2 切换 16 种 | 一条提示文字（30 条/人格池中按日期选） |
| 每日运势 | 显示今日宜/忌/幸运色 | 左"宜"右"忌"大字 + 底部色块 |

数据生成规则：`(date_yyyymmdd + mbti_index) % pool_size` 哈希，同一天反复打开内容稳定。

### 4.3 实用工具（2 个 app）

| 应用 | 行为 | 输出 |
|---|---|---|
| 大字时钟 | 64pt HH:MM + 日期 + 周几 | WiFi 校时（SNTP）写入 ESP32 内部 RTC；deep sleep 保时；PMIC 关机后丢失，下次开机靠 NTP 重新同步 |
| 电子木鱼 | 每按一次/摇一次 +1 功德 + 木鱼音 | 累计功德数显示，NVS 持久化 |

### 4.4 BLE HID（1 个 app，3 子模式）

| 子模式 | 按键映射 |
|---|---|
| PPT 翻页器 | KEY1=PgDn, KEY2 长按=PgUp |
| 相机快门 | KEY1=Vol+ (iOS 相机) |
| 音乐控制 | KEY1=Play/Pause, KEY2 短=Next, KEY2 长=Prev |

进入此 app 时初始化 BLE 广播；退出时 deinit，释放 ~40KB。

### 4.5 设置（1 个 app）

- MBTI 类型选择
- 屏幕亮度（0-100）
- 音量（0-100，含静音）
- 暗色模式（自动跟随时间 / 始终浅 / 始终深）
- 语言（中 / 英）
- WiFi（含重新配网）
- 关于 + 固件版本

## 五、交互约定

M5StickS3 只有 **1 个用户按键**（G11 = 前面板大长条），加 IMU 摇一摇。
双击和长按被 PMIC 占用（关机 / 进入 boot 模式），业务层不能用。

| 手势 | 主菜单 | 应用内 |
|---|---|---|
| BTN 单按 (G11) | 进入选中 app | 触发主操作（翻页/出答案/敲一下） |
| BTN 中等长按 (≥800ms, <3s) | 锁屏 | 返回主菜单 |
| BTN 双击 | — | **不可用**（PMIC 拦截 = 关机） |
| BTN 超长按 ≥3s | — | **不可用**（PMIC 拦截 = boot 模式） |
| 摇一摇 (BMI270 shake) | 下一个 app（光标移动） | 等同 BTN 单按（翻答案、骰子、硬币、敲木鱼连击） |
| 摇晃 + 长按 | — | 设置（保留方案） |

## 五-A、菜单导航补充

由于只有单键，主菜单的"选择 + 进入"两步必须复用：
- **摇一摇 = 光标右移（循环）**
- **BTN 单按 = 进入当前光标的 app**
- **长按 = 返回**

应用内的"模式切换"（如骰子 1/3/5/9 个）通过摇一摇切换，BTN 触发掷骰。

## 六、电源 / 续航目标

- 日常使用 5 分钟/天 + 23h55m 待机 → **≥ 30 天续航**
- 完全关机泄漏 ≤ 30 µA
- USB 接入 → LCD 显示充电图标 + 电压

电源状态：
- L3B 活跃 ~80 mA（UI 亮 + WiFi）
- L3A active ~37 mA（UI 亮 + 无线关）
- 屏暗 ~25 mA
- Light sleep ~3 mA（60s 空闲后）
- Deep sleep ~100 µA（5 min 空闲后，KEY1 RTC GPIO 唤醒）
- 完全关 ~14 µA（KEY1 长按 5s）

## 七、UI 质量基线

LCD 是 135×240 彩色 IPS，要**精雕细琢**（详见 `docs/UI_DESIGN_LANGUAGE.md`）：

- 字体抗锯齿，至少 3 个字号层级（48/32/16）
- 限定 7 色语义色板
- 8px 基线网格
- 切屏 ease-out-cubic 300ms 平滑过渡
- 每次按键都有视觉/音频反馈（10ms beep + 0.1s scale 反馈）
- 暗色模式跟随时间自动切换

不允许直接堆砌默认 LVGL 控件交付。

## 八、非功能要求

- 启动到主菜单 ≤ 2.5s（从冷启动）
- 唤醒到 UI 响应 ≤ 200ms（从 deep sleep）
- 任何按键响应延迟 ≤ 50ms
- 内存占用：堆余量 ≥ 30% 全程（用 USB-CDC console `heap` 命令监控）

## 九、明确不做（V1）

- OTA 升级 — 后续 USB 烧录
- NFC 卡 — 硬件不支持
- 自定义壁纸 / 4 灰度上传 — 彩屏意义不大
- 摇一摇唤醒 — 仅 KEY1 唤醒
- Secure Boot / Flash Encryption
- 手机配套 App
- 云端内容包推送
- 多于中英外的语言

## 十、成功标准

- 干净烧录后开机引导（MBTI + WiFi）顺畅
- 12 个 app 全部可用
- BLE 翻页与 iPad Keynote、iPhone 相机、Mac 音乐配对并控制成功
- 实测续航 ≥ 30 天日常待机
- 一个不懂代码的人按 README 能在 30 分钟内自己烧出来
